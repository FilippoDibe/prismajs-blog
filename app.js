const {
    createPost,
    readPostBySlug,
    updatePostById,
    readPosts,
    deletePostById,
} = require ('./utilities/crud-post.js');

const {
    createTag,
    readTags
} = require('./utilities/crud-tags.js');

const {
    createCategory,
    readCategories 
} = require('./utilities/crud-categories.js')


 categoria.createCategory("sport")
